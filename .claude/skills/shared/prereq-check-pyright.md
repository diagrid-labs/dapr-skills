# Check Pyright language server

Run `pyright --version` (on Windows, retry with `powershell -Command "pyright --version"` if needed). If not installed, inform the user they need to install Pyright with `npm install -g pyright` (or `sudo npm install -g pyright` on macOS/Linux).
